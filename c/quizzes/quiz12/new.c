/******************************************************************************
*	Author:    Yuval 
*	Reviewer : 
*	Date:      
******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#define ARRAYSIZE(x) (sizeof x/sizeof x[0])

#define MAX2(a,b) (a > b) ? a : b
#define MAX3(a,b,c) MAX2(a,b) > MAX2(b,c) ? MAX2(a,b) : MAX2(b,c)

#define TO_LOWER(a) a < 90 ? a + 32 : a

#define OFFSET(s,f) (&(s.f) - &s)    

int foo()
{



	return 0;
}

int main()
{


	return 0;
}

4. size of struct is 16 bytes or 12 byte (depend on size of int)
	
5. new datatype

6. a new struct 

7. size of u1 is 8 bytes

8. reasonable order.

